# Project Handover

## Build

- Build: `platformio run -e 4d_systems_esp32s3_gen4_r8n16`

## Local configuration

Create `include/secrets.h` from `include/secrets.example.h` to enable Wi-Fi
services. Build outputs, `node_modules/` and local credentials are excluded.
